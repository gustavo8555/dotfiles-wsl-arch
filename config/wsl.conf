# Colocar em /etc/wsl.conf (dentro do Arch, precisa de sudo)
# Impede que binarios do Windows (ex: um Salesforce CLI instalado no
# Windows) vazem para o PATH do Linux e sejam executados por engano.
[interop]
appendWindowsPath = false
